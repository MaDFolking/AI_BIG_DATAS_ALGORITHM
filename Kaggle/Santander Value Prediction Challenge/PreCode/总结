通过我们对PCA,SparseRandomgProjection TSVD 降维配合Kmeans RF的聚类分类尝试。
(1)如果降维到100时，再聚类，分类，做特征构造，效果不错，但缺少很多有用特征，且容易过拟合。
(2)如果降维到1000时，再去+SparseRandomgProjection随机投影的效果比降维到500以下要好。更容易防止过拟合，但噪点更多。
(3)PCA处理这种高维稀疏，效果往往不如TSVD,而SparseRandomProjection对结果影响不大，可以加快XGB收敛速度，我们可以把它当成一个辅助作用
(4)GBDT+LR不适合做这种大量高维数据，这次先放弃。

另外，的确证明，线性特征性提取与非线性差距是很大，而XGB LGB特征提取与RF,GBDT特征提取也有一定差距。
这次我们证明线性处理这种高维稀疏矩阵不太好，所以就取消了线性特征提取，而树形特征提取我们继续会做更多尝试，设置LGB的叶子节点权重，让其特征提取更准确。
或者融合四个模型取共同特征，然后再与LGB/XGB并行提取。再做交叉验证，我认为这样可以更加准确，还有效防止过拟合问题。

最后更好的防止过拟合，我们可以采取三种模型融合。
1.正常的模型融合：平均值法，stacking，blending，可以用到企业当中，但模型之间必须结果相近，如果想用RF配合XGB,LGB,往往效果不太好。
2.将结果集设置权重融合，使得结果更平滑，还有效防止模型之间结果有差距的问题，但这个最好不要用在企业当中，但本次比赛比较适合这种。
3.下采样分别用不同模型取得结果，然后给Lasso线性回归设置w权重，最后得到结果，适合数据量比较大的，我们这里训练集不到5000不适合。


PS:比赛第二阶段，有人找到了一些规律特征，是时间特征，并公布了出去，官方说可以使用这些特征，所以我将其做了一个Predict。这个Predict当做一个特征，因为
比赛的评估指标是RMPLSE，所以我将结果log化，然后我会用更细致的方法，进一步通过这个比较特征性(XGB方法比较)，将特征进一步提取，并配合公开的时间特征，
做进一步修改。最后为了防止过拟合，可以用不同模型做不同的结果，然后调权重融合，因为这个方法，使得我在公开LB只有15%的名次，而私人LB取得8%名次(这是最终
结果)。主要原因是很多人模型过拟合严重了。因为过拟合的本质原因，就是正则项前面的特征权重太大，所占据权重比重太大，导致更多的适合你的当前数据，不一定
适合其他数据，所以我们的结果特征权重不能太少，所以需要做增加权重，模型融合平滑化的特点。这些都是为了增加权重数量，减少权重参数，使其适合更多数据集。
