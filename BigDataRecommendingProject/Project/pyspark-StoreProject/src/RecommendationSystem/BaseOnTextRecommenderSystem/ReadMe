这是基于文本内容，并结合其他数据，去向用户推荐价格的推荐系统，属于基于内容的推荐系统。
在本次项目中，我和一位朋友在看过一个高手的代码后使用了并发机制，管道机制，根据业务设定了MLP多神经网络融合 和 嵌入式+CNN多神经网络结合方式去做。
在这里我会在业务模块，技术模块，代码模块做详细解释，并在推荐系统基于内容模块做个总结。
并总结本次项目的缺点不足。
MLP放在了MLP_handle里。
CNN放在CNN_handle里。
handle是原始代码。
