这是我们项目的第三个算法模块，图像处理方向，在一个公司里的19万张，30G图片进行GAN模拟生成类似图片，并进行严格的分类。但由于本人计算机不是GPU。
很难用resnet,desnet,xception,inceptionv4这些比较深的网络进行处理，所以我这里只做了GAN生成图片的代码，存放在GanProject文件夹中。
而分类代码我会将流行的神经网络单独架构都写到ImageClassification这一文件夹中，并最终写个模拟的集合5个比较好的神经网络的模拟代码，
以后有钱了我会换台GPU+大内存的台式机继续完成这一环节!
